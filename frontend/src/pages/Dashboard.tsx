import React from "react";
import Wrapper from "../components/Wrapper";

const Dashboard = () => {
  return (
    <Wrapper>
      <div>Dashboard</div>
    </Wrapper>
  );
};

export default Dashboard;
